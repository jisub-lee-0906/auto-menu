// Compatibility command: review queues were replaced by fail-closed reviewed data.
import './audit_menu_catalog.mjs';
console.log('review_candidates=0; uncertain entries are removed, not held for operating use');
